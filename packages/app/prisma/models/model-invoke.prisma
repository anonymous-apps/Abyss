model ModelInvoke {
  id         String   @id 
  references Json?    
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  // ID of the source that generated this response
  sourceId    String

  // JSON array of parsed messages in the response
  parsedMessages Json

  // Raw output text from the model
  rawOutput   String
} 