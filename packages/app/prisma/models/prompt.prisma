model Prompt {
  id         String   @id 
  references Json?    
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  // Name of the prompt
  name       String

  // Text of the prompt
  text       String

  // Optional JSON field for storing prompt keys and values
  dimensions Json?    
}