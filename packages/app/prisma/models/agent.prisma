model Agent {
  id         String   @id 
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  // Display name of the agent
  name       String
  description String

  // The agent graph as a JSON object
  nodes Json
  connections Json  
  nodeParameters Json
} 