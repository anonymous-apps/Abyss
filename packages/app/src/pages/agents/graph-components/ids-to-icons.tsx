import { Box, HammerIcon, MessageCircle, Sparkles, TextIcon } from 'lucide-react';

export const IdsToIcons = {
    chat: MessageCircle,
    model: Box,
    ai: Sparkles,
    text: TextIcon,
    tool: HammerIcon,
};
