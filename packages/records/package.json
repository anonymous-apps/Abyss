{
    "name": "@abyss/records",
    "version": "0.0.2",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "test": "vitest run",
        "build": "scripts/build.sh",
        "watch": "nodemon --watch src --ext ts --exec 'npm run build'"
    },
    "devDependencies": {
        "@types/node": "^12.20.55",
        "esbuild": "^0.25.4",
        "nodemon": "^3.1.7",
        "typescript": "^4.2.4",
        "vitest": "^3.0.9"
    },
    "peerDependencies": {
        "sqlite3": "^5.1.7"
    }
}
